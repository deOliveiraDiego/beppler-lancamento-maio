# Code Tool — Black Vitalícia

Neste lançamento **não há schema de perfil**. Sofia não identifica aluna vs lead.
`get_links` devolve os dois preços e a página com as duas inscrições.
`get_bonus` não lê `query.perfil`.

Não usar o `$fromAI()` de perfil do TPOC de maio/agosto.

Se no futuro a lista Golden entrar, o lookup é por `sessionId` (telefone) num Code
node gitignored — não via input schema do Agent.
